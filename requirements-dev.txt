# Development & CI dependencies — install with:
#   pip install -r requirements-dev.txt
# This pulls in runtime deps via -r requirements.txt, plus the lint/test toolchain.

-r requirements.txt

# Lint / format / type check
ruff>=0.4,<1.0
black>=24.0,<26.0
mypy>=1.10,<2.0

# Test runner
pytest>=8.0,<9.0

# Type stubs for strict mypy on libraries that don't ship their own
types-requests>=2.31,<3.0
types-psycopg2>=2.9,<3.0
